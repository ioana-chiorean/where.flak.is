{
  "name": "where-flak-is",
  "version": "0.1.2",
  "description": "Where is Flaki?",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.js",
    "publish": "git checkout production && git merge master && git push origin production && git checkout master"
  },
  "author": "Flaki",
  "license": "ISC",
  "dependencies": {
    "express": "^4.14.0",
    "node-fetch": "^1.6.3"
  }
}
